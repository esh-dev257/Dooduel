{
  "name": "dooduel-server",
  "version": "1.0.0",
  "description": "Multiplayer drawing game backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "dev:client": "cd client && npm start",
    "dev:full": "start cmd /k \"nodemon server.js\" && cd client && npm start",
    "install-client": "cd client && npm install",
    "build-client": "cd client && npm run build",
    "setup": "npm install && npm run install-client && npm run build-client",
    "build": "npm run install-client && npm run build-client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
